import { Routes } from '@angular/router';
import { PrayerRequestsComponent } from './prayer-requests.component';

export const PRAYER_REQUESTS_ROUTES: Routes = [
  { path: '', component: PrayerRequestsComponent }
];
